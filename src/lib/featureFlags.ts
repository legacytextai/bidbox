// Feature flags for BidBox.
// Flip ENFORCE_FREE_PROJECT_LIMIT back to `true` to re-enable the 3-project
// free tier cap for non-subscribed users.
export const ENFORCE_FREE_PROJECT_LIMIT = false;
export const FREE_PROJECT_LIMIT = 3;
